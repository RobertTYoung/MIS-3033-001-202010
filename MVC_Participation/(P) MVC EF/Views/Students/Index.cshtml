@model IEnumerable<_P__MVC_EF.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<div class="container">
    <div class="row row-cols-3">
        @foreach (var item in Model)
        {
            @*<tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FavoriteColor)
                </td>
                <td>
                    @Html.ActionLink("Details", "Details", new { id = item.StudentID }) |
                </td>
            </tr>*@
            <a class ="nav-link" href="/Students/Details/@item.StudentID">
                <div class="card" >
                    @*style="width: 18rem;"*@
                    @*<img src="..." class="card-img-top" alt="...">*@
                    <div class="card-body">
                        <p>First Name:      @item.FirstName</p>
                        <p>Last Name:       @item.LastName</p>
                        <p>Favourite Color: @item.FavoriteColor</p>
                        <p>Student ID:      @item.StudentID</p>
                    </div>
                </div>
            </a>
            }
    </div>
    </div>


